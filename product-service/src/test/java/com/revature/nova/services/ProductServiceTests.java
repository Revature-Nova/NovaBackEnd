package com.revature.nova.services;

public class ProductServiceTests {
    /*Test for :
        public List<Product> filterProducts(String type, String value) {
        List<Product> productList = new ArrayList<>();

        switch (type) {
            case "genre":
                productList = repo.findByGenre(value);
                break;
            case "platform":
                productList = repo.findByPlatform(value);
                break;
            case "rating":
                productList = repo.findByRating(value);
                break;
        }
        return productList;
    }
     */

    //Need to test for each case if a list is returned given a valid value (success)
    //Need to test for each case if an invalid value is given to each case (failure)
}
