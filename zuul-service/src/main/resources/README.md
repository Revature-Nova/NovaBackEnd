Create an application.properties and store these values into it

```
server.port=
spring.application.name=

# Configure Eureka Server url
eureka.client.service-url.defaultZone=
eureka.client.fetch-registry=
```
